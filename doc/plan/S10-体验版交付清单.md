# S10 体验版交付清单

1. 微信开发者工具打开本仓库，上传 `cloudfunctions/` 下各函数（云端安装依赖）。
2. `catalogImport` 若尚未导入目录，按该函数 README 再导入。
3. `recognizeHomework` 配置环境变量 `AI_GATEWAY_API_KEY`（中台 Token）。超时 60 秒。
4. 把体验版成员加上家人微信号。
5. 走通：建家庭 → 添加孩子 → 选教材（editionId 来自目录）→ 计时 → 拍照 mock 识别入库 → 补练 → 打星后综合卷。
6. **不要提交微信审核。**
