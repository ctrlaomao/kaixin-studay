# 功能任务清单（一功能一子 Agent）

> 实现阶段 **每个子 agent 只领一张任务卡**。依赖未完成不得开工。接口以 `doc/spec/api.md`（S4 产出）为准。  
> 编排总则见 [`agent-skills-编排.md`](agent-skills-编排.md)。

## 独立性约定

- **文件边界：** 任务卡「允许改动」以外的文件禁止修改。今日页（P01）只通过约定插槽挂组件，禁止把业务逻辑堆进 `today` 一页。
- **云函数：** 一功能至多新增 1 个云函数（或只扩已有函数的明确 action），名称写在任务卡。
- **数据：** 只写本功能集合；跨集合用已有 API，不直接改别人的字段语义。
- **UI：** 只做本功能页面/组件，token 用宪法色值。
- **做完即停：** 不顺手做下一张卡。

```
S1 SPEC → S2 本清单校准 → S3 上下文 → S4 地基
    → F01…F27 按依赖串行（总控一次派一张卡）
    → S8 验证 → S9 评审 → S10 体验版交付
```

## 地基（S4 内仍可拆成 3 个 agent，可选）

| ID | 功能 | 依赖 | 允许改动（预估） |
| --- | --- | --- | --- |
| F00a | Uni-app 四 Tab 空壳 + 设计 token | S3 | 工程脚手架、tabBar、全局样式 |
| F00b | 云开发 ping + 集合名/权限说明 | F00a | cloudfunctions/ping、doc/spec/api.md 初稿 |
| F00c | 示例课时目录 JSON（可被采集结果替换） | F00b | data/catalog 样例 |
| F00d | 目录数据模型对齐平台筛选项（含 platformTag） | F00b | 树 schema |
| F00e | 采集公开课时标题树（可落临时 JSON） | F00d | scripts/catalog-sync |
| F00g | 采集结果写入云数据库 | F00e | catalogImport 分批 upsert |
| F00f | P14 按平台归纳展示（读云库） | F00g | P14 |

## 实现任务卡（细、尽量独立）

| ID | 功能（一句话） | 页面 | 依赖 | 完成定义 |
| --- | --- | --- | --- | --- |
| F01 | 孩子档案：年级、显示名 | P12 | F00a | 能保存/读取一个孩子 |
| F02 | 分科教材版本 + 当前进度章 | P13 | F01, F00g | 选项来自云库目录 |
| F03 | 家庭绑定学生 openid | P12 | F01 | 两个微信挂同一 childId |
| F04 | 纸质作业计时 | P01 插槽 | F01 | 开始/暂停/结束；同时一段；忘关 4h |
| F05 | 拍照多图上传并压缩到云存储 | P02 | F01 | 得到 fileID 列表 |
| F06 | 创建作业批次 homework_batch | P02 | F05 | 批次待核对 |
| F07 | AI 识图云函数（无 Key 则 mock） | 无 UI | F06 | 返回题干/对错/课时/置信度 |
| F08 | 识别核对页（低置信必确认） | P03 | F07, F02 | 未确认低置信不可入库 |
| F09 | 错题入库 wrong_item | P03 | F08 | 错题本能查到新记录 |
| F10 | 打星纯函数 + 单测（无 UI） | — | F00a | 升/降/间隔/人工标记可测 |
| F11 | 新错题写 mastery=1 星 | — | F09, F10 | 入库同时 1 星 |
| F12 | 错题本列表筛选 | P04 | F09 | 按学科/时间列表 |
| F13 | 错题详情 + 改绑课时 | P05 | F12, F02 | 改绑后列表课时名变 |
| F14 | 今日待练组卷（≤10 题） | — | F11, F17 可后补探测 | 返回题目数组 |
| F15 | 补练答题 UI | P08 | F14 | 能选题/填空并交卷 |
| F16 | 补练交卷：改星 + 错题回流 | — | F15, F10 | 对错更新 mastery |
| F17 | 遗漏探测：进度章内未练课时 | — | F02, F11 | 1～3 题并入待练 |
| F18 | 掌握度学科概览 | P06 | F11 | 看各科满星/弱星 |
| F19 | 章节课时树与星 | P07 | F18, F00g | 展开章内课时星 |
| F20 | 在线练习时长写入 practice_record | P08 | F16 | 与纸质计时分开展示 |
| F21 | 今日待办聚合（核对/待练入口） | P01 | F06, F14 | 有入口无重复逻辑 |
| F22 | 心愿：家长增改、主心愿唯一 | P09 | F01 | 排队+进行中 |
| F23 | 孩子许愿待家长同意 | P09 | F22 | 未同意不计入 |
| F24 | 满星进度（排除人工星） | P01/P09 | F18, F22 | 进度条数字正确 |
| F25 | 综合卷组卷 15–25 题 | — | F24 达标 | 覆盖满星课时 |
| F26 | 综合卷作答（中途无解析） | P10 | F25 | 一次交卷 |
| F27 | 综合卷结果：及格待兑 / 不及格冷却 | P11 | F26, F10 | 孩子不能点已兑现 |
| F28 | 家长确认线下兑现 | P09 | F27 | 状态已兑现 |

**F14 与 F17：** F14 可先只含错题课时题；F17 完成后由总控派「F14b 把探测题并入组卷」（仍单独一张卡，见 prompts）。

推荐开工顺序：`F00a→b→d→e→g→f`。F00c 仅占位。F02/F00f 依赖 **F00g 云库已有数据**。

## 提示词

- 通用：[`prompts/F-template.md`](prompts/F-template.md)
- 任务卡：[`prompts/f-cards.md`](prompts/f-cards.md)（复制 **一张** 卡给子 agent）
- 总控：[`prompts/00-orchestrator.md`](prompts/00-orchestrator.md)
