# 功能任务卡（每次只复制一张）

用法：总控复制 `F-template.md` 全文，再复制下面 **一个** `## Fxx` 小节。  
标了 **A+B** 的卡必须派两次，prompt 写明「切片 A」或「切片 B」。波次 A 禁止改 `pages/`。

---

## F00a Uni-app 四 Tab 空壳

- **波次：** B（波次 A 全部完成后再做）。
- **目标：** 微信小程序可预览；tab：今日/错题/掌握/我的；全局色 `#0F766E` `#D97706` `#F4F1EA`。
- **依赖：** F00g。
- **允许改动：** 工程根配置、`frontend/`、四 Tab 骨架、全局样式、tabBar、`wx.cloud.init`（无业务 callFunction）。
- **禁止：** 业务云函数、计时逻辑、页面直写云库。
- **验收：** 开发者工具打开四个 Tab 不报错。

---

## F00b 云开发 ping 与集合约定

- **波次：** A。
- **目标：** `cloudfunctions/ping` 为普通云函数，返回 `{ ok: true }`。`api.md` 列集合名与后续函数名。
- **依赖：** S3。
- **允许改动：** `cloudfunctions/ping/**`、`cloudfunctions/README.md`、`doc/spec/api.md`。
- **禁止：** 仓库根 `cloudfunctions/`；业务写库；改 `pages/`；为 ping 单独做 HTTP 云函数/自建服务器。
- **验收：** 云端测试返回 ok；无密钥入库。

---

## F00c 示例课时目录（占位）

- **波次：** A（可选，可跳过）。
- **目标：** `data/catalog/` 一份样例，字段对齐最终树 schema；采集完成后可被 F00e 覆盖。
- **依赖：** F00b。
- **允许改动：** `data/catalog/**` 样例。
- **禁止：** 下载课件正文。
- **验收：** JSON 含学段-年级-学科-版本-册-章-课时字段占位。

---

## F00h 统一 HTTP 入口（已取消）

- **状态：** 取消。小程序直接对接云开发 `callFunction`，不自建网关。见 ADR-005。

---

## F00d 目录数据模型对齐平台

- **波次：** A。
- **目标：** 定义与同步课堂一致的树：学段、年级、学科、版本、册次、章、课时、`platformTag`（筛选项 id 路径）。写入 `doc/spec/catalog-schema.md`。
- **依赖：** F00b。
- **允许改动：** 仅 schema 文档与 TypeScript/JSON Schema 类型文件。
- **禁止：** 采集爬虫、UI。
- **验收：** 字段能表达平台 defaultTag 那种层级，且打星仍绑课时 id。

---

## F00e 采集各科各版公开目录

- **波次：** A。
- **目标：** 脚本从同步课堂采集初中数/理/化/英已上线各版本的章/课时标题树。可先写成临时 JSON 缓冲。无接口则半自动。
- **依赖：** F00d。
- **允许改动：** `scripts/catalog-sync/**`、可选 `data/catalog/*.json` 缓冲。
- **禁止：** 保存课件、视频、PDF；把缓冲文件当成线上真源。
- **验收：** ≥2 学科各≥1 版本的完整章课时树；对照 URL 写在 README。

---

## F00g 目录写入云数据库

- **波次：** A。
- **目标：** 云函数 `catalogImport` 将采集结果 **分批 upsert** 到 `catalog_edition`、`catalog_lesson`，并写 `catalog_sync_log`。客户端只读这些集合。
- **依赖：** F00e, F00b。
- **允许改动：** `cloudfunctions/catalogImport/**`、api.md、集合权限说明（目录只读、写入仅该函数）。
- **禁止：** 前端直写目录集合；前端 HTTP 以外的读库；单次导入撑爆超时（必须分批）。
- **验收：** 控制台能查到课时文档；P14 未做前可用数据库查询证明。

---

## F00f P14 按平台归纳展示

- **波次：** B。
- **目标：** 筛选顺序与平台相同：年级→学科→版本→册，再展开章、课时列表（名称来自采集数据）。
- **依赖：** F00a, F00g。
- **允许改动：** P14 页面、调用目录 **HTTP**（禁止 `wx.cloud.database`）。
- **禁止：** 另造章节命名；播放平台视频；直连云库。
- **验收：** 切换版本后树与 JSON 一致；与 H5/平台筛选项语义一致。

---

## F01 孩子档案

- **波次：** A+B，派两次。
- **切片 A：** 仅 `childProfile` 与 child 集合（按 familyId 多条）。验收：可列出/新增/更新。禁止改 pages。
- **切片 B：** P12 孩子列表与编辑。验收：能添加第二份档案且互不覆盖。
- **目标：** 显示名、年级；一家庭多名孩子。
- **依赖：** A: F00b；B: F00a + F01A。
- **允许改动：** A 云函数与集合；B 仅 P12。

---

## F02 分科教材版本与进度章

- **波次：** A+B。
- **切片 A：** 写 `child.textbookBySubject`、`progressChapter`，读云库 catalog。禁止 P13。
- **切片 B：** 仅 P13。
- **目标：** 每科选版本（目录有的）；当前学到哪一章。
- **依赖：** A: F01A, F00g；B: F00a + F02A。
- **允许改动：** A 字段与 API；B 仅 P13。
- **验收：** A 云函数分科保存；B 数学/英语可分别保存不同版本。

---

## F03 家庭成员与角色

- **波次：** A+B，**一期必做，不可延后**。
- **切片 A：** 仅 `familyBind`（createFamily / createInvite / joinFamily / me）。切片 B：P12 家庭成员、邀请码、加入、展示本人角色。
- **目标：** 微信号加入一个家庭；`members[{openid, role, childId?}]`。学生成员必须有 childId。邀请码绑定 `inviteChildId`。角色仅 `parent` | `student`。邀请码有过期。加入方须已是体验成员。
- **依赖：** A: F01A；B: F01B。
- **禁止：** 一号多家庭、一号多角色、前端直调云函数绕过 HTTP。
- **验收：** 两号分别调用云函数得到不同 role；学生 redeem 失败；家长可代答；文档写清体验成员 + 邀请步骤。

---

## F04 纸质作业计时

- **波次：** A+B。
- **切片 A：** 仅云函数 `timer`、`timer_session`。切片 B：`HomeworkTimer` 与 P01 插槽。
- **目标：** 选学科、开始/暂停/结束；同时仅一段（按当前 childId）；>4h 忘关标记。
- **依赖：** A: F01A；B: F00a + F04A。
- **禁止：** 监考、读其他 App、把时长算进心愿。
- **验收：** A 连续调用状态正确；B 暂停累计正确；结束可备注。

---

## F05 拍照上传压缩

- **波次：** B。
- **目标：** 多图选择、压缩、上传云存储，返回 fileID[]。关联孩子：成员中仅 1 名学生则默认其 childId；多名学生必须选择后才能提交。
- **依赖：** F01B。
- **允许改动：** P02 选图/上传/当前孩子选择、存储路径约定 `homework/{childId}/`。
- **禁止：** 识别、写错题；多名学生时静默写到错误孩子。
- **验收：** 存储中能看到压缩图；单学生无选择器或选择器预填；双学生未选提交被拦。

---

## F06 作业批次

- **波次：** A+B。
- **切片 A：** 仅 `homeworkBatch`；请求体带 fileIDs + childId（可省略当且仅当家庭仅 1 名学生，服务端填默认）。切片 B：P02 提交按钮。
- **目标：** 学科/日期/来源 + fileID 生成 `homework_batch`（含 childId）status=待核对。
- **依赖：** A: F01A；B: F05。
- **允许改动：** 按切片。
- **验收：** A 库中有 batch；B 今日待核对能列出（列表可先简陋）。

---

## F07 AI 识图

- **波次：** A。
- **目标：** 云函数根据 fileID 返回题目数组。识图默认外部 **DeepSeek** `deepseek-v4-flash-vision-exp`（Key 仅环境变量）。无 Key 返回 `missing_deepseek_key`；`mock:true` 带 `mocked:true`。
- **依赖：** F06A。
- **允许改动：** 仅 `cloudfunctions/recognizeHomework/**` 与 api.md 该节。
- **禁止：** 前端直持模型 Key。
- **验收：** 用一张测试图或 mock 得到结构化 JSON。

---

## F08 识别核对页

- **波次：** B。
- **目标：** P03 展示识别结果；confidence 低于阈值必须点确认/改绑才能过。
- **依赖：** F07, F02B。
- **允许改动：** P03、读 **云库** catalog 选课时。
- **禁止：** 直接写 wrong_item（留给 F09）。
- **验收：** 低置信未确认点「完成」被拦截。

---

## F09 错题入库

- **波次：** A+B。
- **切片 A：** 仅 `wrongItem.create`，入参为已确认题目 JSON。切片 B：P03 完成按钮调用该 API。
- **目标：** 核对通过后写入 `wrong_item`。
- **依赖：** A: F07；B: F08。
- **验收：** 数据库有记录，含 lessonId、errorType。

---

## F10 打星纯函数与测试

- **波次：** A。
- **目标：** 纯函数实现 1–5 星、降星、顽固、间隔 2/7 天、manualOverride。
- **依赖：** F00b。
- **允许改动：** `cloudfunctions/_common/masteryStars.js`（或 ts）+ 仓库 `tests/`。
- **禁止：** 任何页面。
- **验收：** 测试覆盖升、降、人工标记不计心愿（函数返回字段）。

---

## F11 新错写 1 星

- **波次：** A。
- **目标：** F09 成功后调用打星：该课时 mastery=1（已有更高星则按 SPEC，默认新错降或不低于 1）。
- **依赖：** F09A, F10。
- **允许改动：** 仅衔接 F09 云函数内调用 F10；`mastery` 集合。
- **验收：** 新错题对应课时为 1 星。

---

## F12 错题本列表

- **波次：** A+B。
- **切片 A：** 仅 `wrongItem.list`。切片 B：仅 P04。
- **目标：** 按学科/时间筛选列表，展示裁剪/错因/星。
- **依赖：** A: F09A；B: F00a + F12A。
- **验收：** 能看到 F09 入库的题。

---

## F13 错题详情改绑

- **波次：** A+B。
- **切片 A：** 仅 `wrongItem.updateLesson`。切片 B：仅 P05。
- **目标：** 原图、改 lessonId。
- **依赖：** A: F12A, F02A；B: F13A + F02B。
- **验收：** 改绑后列表课时名更新。

---

## F14 今日待练组卷

- **波次：** A。
- **目标：** 云函数根据错题课时出 3–8 题，总数预留≤10。
- **依赖：** F11。
- **允许改动：** `practiceCompose`、题目 mock/生成。
- **禁止：** 答题 UI（F15）、遗漏探测（F17）。
- **验收：** 返回题目 JSON，含 lessonId。

---

## F14b 待练并入遗漏题

- **波次：** A。
- **目标：** 组卷结果拼上 F17 的 1–3 题，总数≤10。
- **依赖：** F14, F17。
- **允许改动：** 仅 `practiceCompose`。
- **验收：** 有遗漏课时时数组含探测题。

---

## F15 补练答题页

- **波次：** B。
- **目标：** P08 展示 F14 题目，选择/填空，不显示答案直至交卷。
- **依赖：** F14, F00a。
- **允许改动：** P08 UI、本地作答状态。
- **禁止：** 改 mastery（F16）。
- **验收：** 能选完并点交卷（交卷可先只 console/待 F16）。

---

## F16 补练交卷改星回流

- **波次：** A+B。
- **切片 A：** 仅 `practiceSubmit`，入参为答案 JSON。切片 B：P08 结果小结。
- **目标：** 判分；对/错调用 F10；错的进错题本。
- **依赖：** A: F14, F10；B: F15。
- **验收：** 错题回流；星变化符合规则。

---

## F17 遗漏探测

- **波次：** A。
- **目标：** 当前进度章内无对错记录的课时，生成 1–3 道探测题。
- **依赖：** F02A, F11。
- **允许改动：** `gapDetect` 云函数。
- **禁止：** 改组卷主流程以外的 UI。
- **验收：** 进度章外课时不会入选。

---

## F18 掌握度概览

- **波次：** A+B。
- **切片 A：** 仅 `mastery.overview`。切片 B：仅 P06。
- **目标：** 分科弱星/满星数量。
- **依赖：** A: F11；B: F00a + F18A。
- **验收：** 与数据库星一致。

---

## F19 章节课时树

- **波次：** B。
- **目标：** P07 展开章→课时星；疑似遗漏标记。
- **依赖：** F18B, F00g, F17 标记可选。
- **允许改动：** P07。
- **验收：** 能看到采集目录上的星。

---

## F20 在线练习时长

- **波次：** A+B。
- **切片 A：** 写入 `practice_record`。切片 B：P01 文案。
- **目标：** 补练/综合卷 durationSec 写入；展示与纸质分开。
- **依赖：** A: F16A；B: F16B。
- **验收：** 补练一次后在线时长增加。

---

## F21 今日待办聚合

- **波次：** B。
- **目标：** P01 待核对、待练入口，只组装 F06/F14 状态。
- **依赖：** F06B, F14, F00a。
- **允许改动：** P01 待办区块组件。
- **禁止：** 复制识别/组卷逻辑。
- **验收：** 有批次显示去核对；有待练显示去答题。

---

## F22 心愿家长 CRUD（**二期，一期禁止实现**）

- **波次：** A+B。
- **切片 A：** `wish` 集合、`wish.save`。切片 B：P09 家长部分。
- **目标：** 添加心愿、**每个孩子**主心愿唯一、排队。
- **依赖：** A: F01A；B: F00a + F22A。
- **禁止：** 考试、支付。
- **验收：** 不能同时两个 active 主心愿。

---

## F23 孩子许愿（**二期，一期禁止实现**）

- **波次：** A+B。
- **切片 A：** 仅 `wish.propose`。切片 B：P09 许愿入口。
- **目标：** 许愿 status=待家长同意。
- **依赖：** A: F22A；B: F22B。
- **验收：** 未同意不出现在可兑进度。

---

## F24 满星统计

- **波次：** A+B。
- **切片 A：** 非人工 5 星课时计数，供综合卷门槛。切片 B：P01 可考入口（无心愿条）。
- **目标：** 不写 `wish` 集合。
- **依赖：** A: F18A；B: F00a + F24A。
- **验收：** 人工改满星不计入。

---

## F25 综合卷组卷

- **波次：** A。
- **目标：** 有 ≥1 个非人工 5 星课时则可组；15–25 题覆盖满星课时。
- **依赖：** F24A。
- **允许改动：** `examCompose`。
- **禁止：** 答题页。
- **验收：** 未达标调用返回错误码。

---

## F26 综合卷作答

- **波次：** B。
- **目标：** P10 答题，中途无解析。
- **依赖：** F25, F00a。
- **允许改动：** P10。
- **验收：** 交卷前看不到答案。

---

## F27 综合卷结果

- **波次：** A+B。
- **切片 A：** 仅 `examSubmit`，入参为答卷 JSON。切片 B：仅 P11。
- **目标：** ≥80% 记及格（**无待兑**）；否则降星、回流、3 天冷却。
- **依赖：** A: F25, F10；B: F26。
- **验收：** 不及格可走冷却；响应无 redeem。

---

## F28 家长兑现（**二期，一期禁止实现**）

- **波次：** A+B。
- **切片 A：** 仅 `wish.redeem`。切片 B：P09/P11 家长按钮。
- **目标：** 待兑现 → 已兑现。
- **依赖：** A: F27A；B: F27B。
- **验收：** 仅家长角色成功。
