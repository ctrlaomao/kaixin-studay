# S9 评审（简）

- 前端为微信原生小程序四 Tab（`frontend/`），未另起 Uni-app 工程，与 `project.config.json` 的 miniprogramRoot 一致。
- 云函数按卡分目录；打星纯函数有 `npm test`。
- 上传作业走 `wx.cloud.uploadFile` 到 `homework/{childId}/`（F05）；与旧 api.md「禁止页面直传」以任务卡 F05 为准。
- 综合卷页无兑现按钮。
