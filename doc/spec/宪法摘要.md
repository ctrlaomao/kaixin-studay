# 宪法摘要（子 agent 置顶）

1. 体验版家庭小程序，家长主操，不正式上架。  
2. 打星单位 = 同步课堂 **课时**。  
3. 不做：下载课件视频电子书、支付商城、监考计时、老师端。允许采集同步课堂公开目录并按平台层级展示。一期不做费曼讲解模块（二期规划）。一期：微信号加入家庭，每号一个角色（家长/学生）。多名学生时家长须选择关联孩子，仅一名则默认。  
4. 微信云开发即后端，不自建服务器。小程序 `callFunction`。禁止客户端直写云库。Key 只在云端。云函数目录：`cloudfunctions/`。  
5. UI：`#0F766E` / `#D97706` / `#F4F1EA`，页面 P01–P14。  
6. 人工改星默认不计满星可考（二期心愿同此）；综合卷及格 80%；计时忘关 4 小时。一期不做兑心愿。  
7. 只改当前任务卡允许的文件，做完即停。  
8. 无验证证据不算完成。界面中文。Git 提交说明用中文（type 可用 feat/fix，标题与正文中文）。
